<Window x:Class="Remnant2UnlockerApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remnant 2 Item Spawner"
        Height="760"
        Width="1320"
        WindowStartupLocation="CenterScreen"
        Background="#101116" MinHeight="760" MinWidth="1320" AutomationProperties.HelpText="Test" ResizeMode="CanMinimize" Topmost="{Binding AlwaysOnTop}">

    <Window.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonRoot"
                                Background="{TemplateBinding Background}"
                                CornerRadius="9"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonRoot" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.985" ScaleY="0.985"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}"/>

        <Style x:Key="QuantityArrowButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="19"/>
            <Setter Property="Background" Value="#202637"/>
            <Setter Property="Foreground" Value="#D8DCE5"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style x:Key="QuantityTextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="58"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#252936"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#33405F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9,0,0,9">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="Root" Property="Background" Value="#1F2D4A"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AllCategoryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="13,0"/>
            <Setter Property="Background" Value="#1A1D27"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="CategoryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="#1A1D27"/>
            <Setter Property="Foreground" Value="#D8DCE5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ProfessionalExpanderStyle" TargetType="Expander">
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border x:Name="Outer"
                                Background="#171922"
                                BorderBrush="#273044"
                                BorderThickness="1"
                                CornerRadius="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <ToggleButton x:Name="HeaderSite"
                                              Grid.Row="0"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand"
                                              Padding="12,11">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="HeaderRoot"
                                                    Background="Transparent"
                                                    CornerRadius="12"
                                                    Padding="{TemplateBinding Padding}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="22"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border x:Name="ChevronCircle"
                                                            Grid.Column="0"
                                                            Width="17"
                                                            Height="17"
                                                            CornerRadius="8.5"
                                                            Background="#2F3A56"
                                                            VerticalAlignment="Center">
                                                        <Path x:Name="Chevron"
                                                              Data="M 4 6 L 8.5 10.5 L 13 6"
                                                              Stroke="#D8DCE5"
                                                              StrokeThickness="2"
                                                              StrokeStartLineCap="Round"
                                                              StrokeEndLineCap="Round"
                                                              RenderTransformOrigin="0.5,0.5">
                                                            <Path.RenderTransform>
                                                                <RotateTransform Angle="0"/>
                                                            </Path.RenderTransform>
                                                        </Path>
                                                    </Border>

                                                    <ContentPresenter Grid.Column="1"
                                                                      Margin="8,0,0,0"
                                                                      HorizontalAlignment="Left"
                                                                      VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="HeaderRoot" Property="Background" Value="#1D2231"/>
                                                    <Setter TargetName="ChevronCircle" Property="Background" Value="#33405F"/>
                                                </Trigger>

                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="HeaderRoot" Property="Background" Value="#1A1D27"/>
                                                    <Setter TargetName="ChevronCircle" Property="Background" Value="#3B82F6"/>
                                                    <Setter TargetName="Chevron" Property="Stroke" Value="#FFFFFF"/>
                                                    <Setter TargetName="Chevron" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <RotateTransform Angle="180"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>

                                    <TextBlock Text="{TemplateBinding Header}"
                                               Foreground="#F2F2F2"
                                               FontSize="14"
                                               FontWeight="Bold"/>
                                </ToggleButton>

                                <Border x:Name="ContentHost"
                                        Grid.Row="1"
                                        Padding="10,0,10,10"
                                        Visibility="Collapsed"
                                        Opacity="0">
                                    <ContentPresenter/>
                                </Border>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ContentHost" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentHost" Property="Opacity" Value="1"/>
                                <Setter TargetName="Outer" Property="Background" Value="#171922"/>
                                <Setter TargetName="Outer" Property="BorderBrush" Value="#273044"/>
                                <Setter TargetName="Outer" Property="CornerRadius" Value="12"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ItemCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A1D27"/>
            <Setter Property="BorderBrush" Value="#232838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="10,6"/>
            <Setter Property="MinHeight" Value="92"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                    <Setter Property="Background" Value="#1F2D4A"/>
                    <Setter Property="BorderBrush" Value="#3B82F6"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsMouseOver}" Value="True">
                    <Setter Property="Background" Value="#202637"/>
                    <Setter Property="BorderBrush" Value="#33405F"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="#1A1D27"/>
            <Setter Property="Foreground" Value="#D8DCE5"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#171922" Padding="18">
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="46"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="REMNANT 2"
                           Foreground="#F1F1F1"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,0,0,4"/>

                        <TextBlock Text="Unlocker Bridge"
                           Foreground="#8F95A3"
                           FontSize="13"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                        Content="⚙"
                        Click="OpenSettings_Click"
                        Style="{StaticResource IconButtonStyle}"/>
                </Grid>

                <ScrollViewer VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <Button Content="All"
                                Command="{Binding SelectTypeCommand}"
                                CommandParameter="All"
                                Style="{StaticResource AllCategoryButtonStyle}"/>

                        <ItemsControl ItemsSource="{Binding CategoryGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Expander Header="{Binding Name}"
                                              Style="{StaticResource ProfessionalExpanderStyle}"
                                              IsExpanded="True">
                                        <ItemsControl ItemsSource="{Binding Types}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding}"
                                                            Command="{Binding DataContext.SelectTypeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource CategoryButtonStyle}"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Expander>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="26">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="42"
                         FontSize="15"
                         Padding="14,9"
                         Background="#1A1D27"
                         Foreground="#F2F2F2"
                         BorderBrush="#303645"
                         BorderThickness="1"/>

                <Button Grid.Column="1"
                        Content="Reload"
                        Command="{Binding ReloadCommand}"
                        Height="42"
                        Margin="12,0,0,0"
                        Background="#252936"/>

                </Grid>

            <ListView Grid.Row="1"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem}"
                      Background="#151821"
                      BorderBrush="#303645"
                      BorderThickness="1"
                      Foreground="#F2F2F2"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ItemCardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="82"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="432"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Width="64"
                                        Height="64"
                                        CornerRadius="12"
                                        Background="#252936"
                                        ClipToBounds="True"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                    <Grid>
                                        <TextBlock Text="?"
                                                   Foreground="#8F95A3"
                                                   FontSize="22"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasImage}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <Image Source="{Binding Image}"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="4">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasImage}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>

                                        <ProgressBar Width="30"
                                                     Height="30"
                                                     IsIndeterminate="True"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center">
                                            <ProgressBar.Style>
                                                <Style TargetType="ProgressBar">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsImageLoading}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ProgressBar.Style>
                                        </ProgressBar>
                                    </Grid>
                                </Border>

                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Margin="8,0,16,0">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="#F2F2F2"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Text="{Binding Type}"
                                               Foreground="#8F95A3"
                                               FontSize="13"
                                               Margin="0,6,0,0"/>
                                </StackPanel>

                                <Border Grid.Column="2"
                                        Background="#252936"
                                        CornerRadius="12"
                                        Padding="10,5"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Type}"
                                               Foreground="#D8DCE5"
                                               FontSize="12"/>
                                </Border>

                                <Grid Grid.Column="3"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="24"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <Button Content="Spawn"
                                            Height="40"
                                            Width="92"
                                            Background="#2563EB"
                                            Click="SpawnItem_Click"/>
                                        <Button Content="Force"
                                            Height="40"
                                            Width="92"
                                            Margin="10,0,0,0"
                                            Background="#7C2D12"
                                            Click="ForceConsoleSpawnItem_Click"/>
                                        <Button Content="Copy"
                                            Height="40"
                                            Width="92"
                                            Margin="10,0,0,0"
                                            Background="#3A3F51"
                                            Click="CopyCommand_Click"/>
                                    </StackPanel>

                                    <Border Grid.Column="1"
                                        Width="1"
                                        Height="34"
                                        Background="#303645"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                                    <Button Grid.Column="2"
                                        Content="Wiki"
                                        Height="40"
                                        Width="82"
                                        Background="#252936"
                                        Foreground="#D8DCE5"
                                        Click="OpenWiki_Click"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="2" Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="390"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusText}"
                           Foreground="#B8BFCC"
                           VerticalAlignment="Center"
                           FontSize="14"/>

                <Border Grid.Column="1"
                        Background="#1A1D27"
                        BorderBrush="#273044"
                        BorderThickness="1"
                        CornerRadius="10"
                        Height="44"
                        Margin="8,0,0,0"
                        Padding="12,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="92"/>
                        </Grid.ColumnDefinitions>

                        <Ellipse Grid.Column="0"
                                 Width="8"
                                 Height="8"
                                 VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#DC2626"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGamePathValid}" Value="True">
                                            <Setter Property="Fill" Value="#22C55E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>

                        <StackPanel Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Margin="8,0,8,0">
                            <TextBlock Text="{Binding GamePathStatus}"
                                       Foreground="#F2F2F2"
                                       FontSize="12"
                                       FontWeight="SemiBold"/>

                            <TextBlock Text="{Binding GamePathDisplay}"
                                       Foreground="#8F95A3"
                                       FontSize="11"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <Button Grid.Column="2"
                                Content="Browse"
                                Command="{Binding SelectGamePathCommand}"
                                Height="30"
                                Background="#252936"
                                Foreground="#D8DCE5"/>
                    </Grid>
                </Border>

                <Button Grid.Column="2"
                        Content="Spawn Group"
                        Command="{Binding UnlockGroupCommand}"
                        Height="44"
                        Margin="8,0,0,0"
                        Background="#3A3F51"/>
            </Grid>
        </Grid>
    </Grid>
</Window>