<Window x:Class="Remnant2UnlockerApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="380"
        Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#101116">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#252936"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#33405F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1F2D4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="18"
            Padding="18"
            Background="#1A1D27"
            BorderBrush="#33405F"
            BorderThickness="1"
            CornerRadius="16">

        <StackPanel>
            <TextBlock Text="Settings"
               Foreground="#F2F2F2"
               FontSize="20"
               FontWeight="Bold"
               Margin="0,0,0,18"/>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="58"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                   Text="Always on top"
                   Foreground="#D8DCE5"
                   FontSize="14"
                   VerticalAlignment="Center"/>

                <ToggleButton Grid.Column="1"
                      IsChecked="{Binding AlwaysOnTop}"
                      Width="52"
                      Height="28"
                      Cursor="Hand">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Track"
                                Width="52"
                                Height="28"
                                CornerRadius="14"
                                Background="#252936"
                                BorderBrush="#33405F"
                                BorderThickness="1">
                                <Ellipse x:Name="Thumb"
                                 Width="20"
                                 Height="20"
                                 Fill="#8F95A3"
                                 HorizontalAlignment="Left"
                                 Margin="4,0,0,0"/>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="#2563EB"/>
                                    <Setter TargetName="Track" Property="BorderBrush" Value="#3B82F6"/>
                                    <Setter TargetName="Thumb" Property="Fill" Value="#FFFFFF"/>
                                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                </Trigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Track" Property="BorderBrush" Value="#60A5FA"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </Grid>

            <TextBlock Text="Wiki"
               Foreground="#8F95A3"
               FontSize="12"
               FontWeight="SemiBold"
               Margin="0,0,0,10"/>

            <Border Background="#252936"
                BorderBrush="#33405F"
                BorderThickness="1"
                CornerRadius="12"
                Padding="4"
                Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <RadioButton Grid.Column="0"
                         Content="wiki.gg"
                         GroupName="WikiSelection"
                         IsChecked="{Binding IsWikiGgSelected}"
                         Cursor="Hand"
                         Foreground="#F2F2F2"
                         FontWeight="SemiBold"
                         HorizontalContentAlignment="Center">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="Root"
                                    Height="34"
                                    CornerRadius="9"
                                    Background="Transparent">
                                    <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="#2563EB"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="#33405F"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsChecked" Value="True"/>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Root" Property="Background" Value="#2563EB"/>
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>

                    <RadioButton Grid.Column="1"
                         Content="Fextralife"
                         GroupName="WikiSelection"
                         IsChecked="{Binding IsFextralifeSelected}"
                         Cursor="Hand"
                         Foreground="#F2F2F2"
                         FontWeight="SemiBold"
                         HorizontalContentAlignment="Center">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="Root"
                                Height="34"
                                CornerRadius="9"
                                Background="Transparent">
                                    <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="#2563EB"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="#33405F"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsChecked" Value="True"/>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Root" Property="Background" Value="#2563EB"/>
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>
                </Grid>
            </Border>

            <TextBlock Text="Hotkeys"
               Foreground="#8F95A3"
               FontSize="12"
               FontWeight="SemiBold"
               Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                   Text="Console key"
                   Foreground="#D8DCE5"
                   FontSize="14"
                   VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                    Content="{Binding ConsoleKeyDisplay}"
                    Command="{Binding StartConsoleKeyCaptureCommand}"
                    PreviewKeyDown="ConsoleKeyButton_PreviewKeyDown"
                    Focusable="True"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                   Text="Teleport"
                   Foreground="#D8DCE5"
                   FontSize="14"
                   VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                    Content="{Binding TeleportHotkeyDisplay}"
                    Command="{Binding StartTeleportHotkeyCaptureCommand}"
                    PreviewKeyDown="TeleportHotkeyButton_PreviewKeyDown"
                    PreviewMouseDown="TeleportHotkeyButton_PreviewMouseDown"
                    Focusable="True"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>